---
title: "Test Repo"
date: 2017-07-02 06:05:52 
author: shajeebhameed
layout: page
slug: test-repo
status: publish
---

**User API Test** [TestFixture] public class UserApiTest { private string ErrorMessage; string TestName = "Test Name"; string TestComment = "Test User Comment"; IRepository<User> _objRepo; public UserApiTest() { _objRepo = new GenericRepository<User>("user"); }   //[Test] //public void TestPost() //{ // var objUser = new User() { fname = txtFname.Text, lname = txtLname.Text, age = Int16.Parse(txtAge.Text) }; // var objAddress = new Address() { address1 = txtAddress1.Text, address2 = txtAddress2.Text, phone = txtPhone.Text, city = txtCity.Text, state = txtState.Text, country = txtCountry.Text, pincode = txtPinCode.Text }; // var newUser = new User() { name = TestName, comment = TestComment }; // var savedUser = _objRepo.Save(string.Empty, newUser, out ErrorMessage); // Assert.IsTrue(savedUser.name.Equals(newUser.name), "User 'name' is not matching..."); // Assert.IsTrue(savedUser.comment.Equals(newUser.comment), "User 'comment' is not matching..."); //} //[Test] //public void TestPUT() //{ // var getCusts = _objRepo.GetAll(out ErrorMessage); // Assert.Greater(getCusts.Count(), 0, "NO Saved Users found..."); // var updateCust = getCusts.FirstOrDefault(c => c.name == TestName && c.comment == TestComment); // string updateName = TestName + "1"; // string updateComment = TestComment + "1"; // updateCust.name = updateName; // updateCust.comment = updateComment; // var updatedUser = _objRepo.Save(updateCust._id, updateCust, out ErrorMessage); // Assert.IsTrue(updatedUser.name.Equals(updateName), "User 'name' is not matching..."); // Assert.IsTrue(updatedUser.comment.Equals(updateComment), "User 'comment' is not matching..."); // updatedUser.name = TestName; // updatedUser.comment = TestComment; // var nextUser = _objRepo.Save(updatedUser._id, updatedUser, out ErrorMessage); // Assert.IsTrue(nextUser.name.Equals(TestName), "User 'name' is not matching..."); // Assert.IsTrue(nextUser.comment.Equals(TestComment), "User 'comment' is not matching..."); //} [Test] public void TestGETALL() { var getUsers = _objRepo.GetAll(out ErrorMessage); Assert.Greater(getUsers.Count(), 0, "NO user found in the DB..."); } [Test] public void TestGETByID() { var getUsers = _objRepo.GetAll(out ErrorMessage); Assert.Greater(getUsers.Count(), 0, "NO user found in the DB..."); var getUser = _objRepo.GetById(getUsers.FirstOrDefault()._id, out ErrorMessage); Assert.IsNotNull(getUser, "NULL object found....ErrorMessage:" + ErrorMessage); Assert.IsNotEmpty(getUser.fname, "Name not found...Incorrect data...ErrorMessage:" + ErrorMessage); } [Test] public void TestGETmany() { var getUsers = _objRepo.GetMany("Rem", SearchOption.ContainsWith, out ErrorMessage); Assert.Greater(getUsers.Count(), 0, "NO user found in the DB..."); } //[Test] //public void TestDELETE() //{ // var getCusts = _objRepo.GetAll(out ErrorMessage); // var delCusts = getCusts.Where(c => c.name == TestName && c.comment == TestComment); // foreach (var cust in delCusts) { // var result = _objRepo.Delete(cust._id, out ErrorMessage); // Assert.IsTrue(result, "User object delete failed..."); // } //} } **Customer API Test** [TestFixture] public class CustomerApiTest { private string ErrorMessage; string TestName = "Test Name"; string TestComment = "Test User Comment"; IRepository<Customer> _objRepo; public CustomerApiTest() { _objRepo = new GenericRepository<Customer>("customer"); }   [Test] public void TestPost() { var newCustomer = new Customer() { name = TestName, comment = TestComment }; var savedCustomer = _objRepo.Add(newCustomer, out ErrorMessage); Assert.IsTrue(savedCustomer.name.Equals(newCustomer.name), "Customer 'name' is not matching..."); Assert.IsTrue(savedCustomer.comment.Equals(newCustomer.comment), "Customer 'comment' is not matching..."); } [Test] public void TestPUT() { var getCusts = _objRepo.GetAll(out ErrorMessage); Assert.Greater(getCusts.Count(), 0, "NO Saved Customers found..."); var updateCust = getCusts.FirstOrDefault(c => c.name == TestName && c.comment == TestComment); string updateName = TestName + "1"; string updateComment = TestComment + "1"; updateCust.name = updateName; updateCust.comment = updateComment; var updatedCustomer = _objRepo.Add(updateCust, out ErrorMessage); Assert.IsTrue(updatedCustomer.name.Equals(updateName), "Customer 'name' is not matching..."); Assert.IsTrue(updatedCustomer.comment.Equals(updateComment), "Customer 'comment' is not matching..."); updatedCustomer.name = TestName; updatedCustomer.comment = TestComment; var nextCustomer = _objRepo.Update(updatedCustomer._id, updatedCustomer, out ErrorMessage); Assert.IsTrue(nextCustomer.name.Equals(TestName), "Customer 'name' is not matching..."); Assert.IsTrue(nextCustomer.comment.Equals(TestComment), "Customer 'comment' is not matching..."); } [Test] public void TestGETALL() { var getCusts = _objRepo.GetAll(out ErrorMessage); Assert.Greater(getCusts.Count(), 0, "NO Saved Customers found..."); } [Test] public void TestGETByID() { var getCusts = _objRepo.GetAll(out ErrorMessage); var cust = getCusts.FirstOrDefault(c => c.name == TestName && c.comment == TestComment); Assert.IsNotNull(cust, "Customer object found NULL"); var getUpdatedCustomer = _objRepo.GetById(cust._id, out ErrorMessage); Assert.IsTrue(getUpdatedCustomer.name.Equals(TestName), "Customer 'name' is not matching..."); Assert.IsTrue(getUpdatedCustomer.comment.Equals(TestComment), "Customer 'comment' is not matching..."); } [Test] public void TestDELETE() { var getCusts = _objRepo.GetAll(out ErrorMessage); var delCusts = getCusts.Where(c => c.name == TestName && c.comment == TestComment); foreach (var cust in delCusts) { var result = _objRepo.Delete(cust._id, out ErrorMessage); Assert.IsTrue(result, "Customer object delete failed..."); } } [Test] public void TestGETCust() { var getCustomer = _objRepo.GetById("59561c8d14febb231c2db134", out ErrorMessage); Assert.IsTrue(getCustomer.name.Equals(TestName), "Customer 'name' is not matching..."); Assert.IsTrue(getCustomer.comment.Equals(TestComment), "Customer 'comment' is not matching..."); } }
