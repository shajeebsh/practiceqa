---
title: "Jenkins Groovy Mutithreading 2 Array"
date: 2020-02-11 18:23:29 
author: shajeebhameed
layout: page
slug: jenkins-magic
status: publish
---

_Active Choices Parameter_

This is used for populating a dropdown by fetching some information from the given URL

//

// Set up Koms .

//

Authenticator.setDefault (new Authenticator() {

protected PasswordAuthentication getPasswordAuthentication() {

return new PasswordAuthentication ("test123124", "test123124".toCharArray());

}

});

//

// Get the URL for the latest release.

//

downloadPageURL7_6_0 = "https://www.test.com/url/build/test7.6.1"

downloadPageURL7_5_1 = "https://www.test.com/url/build/test7.5.1"

//

// Create the list of builds.

//

list = getChoiceList(downloadPageURL7_6_0 )

list += getChoiceList(downloadPageURL7_5_1 )

list += "7.5.0.37"

list += "7.4.0.55"

list += "7.3.0.35"

def getChoiceList(downloadPageURL)

{

def map = [:]

def matcher = downloadPageURL.toURL().text =~ /a href="(\d.\d.\d.\d+)\/">.*>.*(\d\d-.*:\d\d)/

def returnVal = matcher.collect{it[1]}

} _Extended Choices Parameter_ This is used for adding multiple row based on need **Name: SelectedTestGroups** import net.sf.json.JSONObject; def testGroups = '''{ disable_edit_json:true, disable_properties: true, no_additional_properties: true, disable_collapse: true, disable_array_add: false, disable_array_delete: false, disable_array_reorder: true, theme: "bootstrap2", iconlib:"None", schema: { "type": "object", "title": "Select Test Groups", "properties": { "Folders": { "type": "array", "format": "table", "title": "Test Groups", "items": { "type": "object", "title": "Test Groups", "properties": { "TestGroups": { "type": "string", "enum": [ "All", "Group1", "Group2", "Group3", "Group4", "Group5", "Group6","Group7","Group8", ] }, } } } } }, startval:{ "Folders": [ { "Folder": "All", } ] } }''' 

return JSONObject.fromObject(testGroups)

_Extended Choices Parameter_ This is used for adding multiple row with different check boxes **Name: TestMachines** import net.sf.json.JSONObject;  def VM01 = "VM01" def VM02 = "VM02" def VM03 = "VM03" def VM04 = "VM04" def VM05 = "VM05" def VM06 = "VM06" def VM07 = "VM07" def VM08 = "VM08" def VM09 = "VM09" def VM10 = "VM10" def VM11 = "VM11" def VM12 = "VM12" def testMachines = '''{ disable_edit_json:true, disable_properties: true, no_additional_properties: true, disable_collapse: true, disable_array_add: false, disable_array_delete: false, disable_array_reorder: true, theme: "bootstrap2", iconlib:"None", schema: { "type": "object", "title": "Test Machine Specification", "properties": { "Folders": { "type": "array", "format": "table", "title": "Test Machines", "items": { "type": "object", "title": "Group", "properties": { "Group": { "type": "string", "enum": [ "Options01", "Options02", ] }, "''' + VM01 + '''":{ "type": "checkbox" }, "''' + VM02 + '''":{ "type": "checkbox" }, "''' + VM03 + '''":{ "type": "checkbox" }, "''' + VM04 + '''":{ "type": "checkbox" }, "''' + VM05 + '''":{ "type": "checkbox" }, "''' + VM06 + '''":{ "type": "checkbox" }, "''' + VM07 + '''":{ "type": "checkbox" }, "''' + VM08 + '''":{ "type": "checkbox" }, "''' + VM09 + '''":{ "type": "checkbox" }, "''' + VM10 + '''":{ "type": "checkbox" }, "''' + VM11 + '''":{ "type": "checkbox" }, "''' + VM12 + '''":{ "type": "checkbox" }, } } } } }, startval:{ "Folders": [ { "Folder": "D01", } ] } }''' return JSONObject.fromObject(testMachines) 

_**Reading SeletectedTestGroups and TestMachines in the Build section**_

import hudson.model.*

import jenkins.model.* import groovy.json.JsonSlurper import groovy.transform.Field import jenkins.model.Jenkins import java.util.concurrent.* import hudson.AbortException @Field def TEST_SET_WAITING = "Waiting" @Field def TEST_SET_IN_PROGRESS = "InProgress" @Field def TEST_SET_COMPLETE = "Complete" @Field def ALL = true @Field def AVAILABLE = true // Get the simple parameters. def RunID= "${build.getBuildVariables().get("RunID")}" def Project = build.getBuildVariables().get("Project") def Environment = build.getBuildVariables().get("Environment") def MAK__7_6_0_Build = build.getBuildVariables().get("MAK__7_6_0_Build") // Get the test machines parameters defined in JSON. def JSONTestMachines = build.getBuildVariables().get('TestMachines') def ParsedJSONTestMachines = new JsonSlurper().parseText(JSONTestMachines) def TestMachines = [] ParsedJSONTestMachines["Folders"].each() { def group = it["Group"] it.each() { field -> if (!field.getKey().equals("Group") && field.getValue()) { def name = "${group} ${field.getKey()}" def machineAlreadyAdded = false; TestMachines.each() { machine -> if (machine.Name == name) { machineAlreadyAdded = true } } if (!machineAlreadyAdded) { TestMachines.add(new TestMachine(name, true)) } } } } println "---------------------------SelectedTests-----------------------------------------" def JSONselectedTestGroups = build.getBuildVariables().get('SelectedTestGroups') def ParsedJSONselectedTestGroups = new JsonSlurper().parseText(JSONselectedTestGroups) def SelectedGroups = [] def allSelected = false; // Get the test sets based on group selections. def TestSets = [] // For latest version MAK_Version = 'TRMAK__X_X' for other versions 'TRMAK__7_5_0', 'TRMAK__7_4_0' etc def MAK_Version = "TRMAK__7_5_0" ParsedJSONselectedTestGroups["Folders"].each() { it.each() { field -> if (field.getKey().equals("TestGroups") && field.getValue()) { def selectedGroup = field.getValue() if (selectedGroup.equals(GROUPS.ALL)) { allSelected = true SelectedGroups.add(selectedGroup); //TestSets are added based on duration and priority TestSets.add(new TestSet("Options_" + GROUPS.GROUP1, MAK_Version, MAK_Version, MAK_Version, GetTestsByGroup(GROUPS.GROUP1))) TestSets.add(new TestSet("Options_" + GROUPS.GROUP4, MAK_Version, MAK_Version, MAK_Version, GetTestsByGroup(GROUPS.GROUP4))) TestSets.add(new TestSet("Options_" + GROUPS.GROUP2, MAK_Version, MAK_Version, MAK_Version, GetTestsByGroup(GROUPS.GROUP2))) TestSets.add(new TestSet("Options_" + GROUPS.GROUP3, MAK_Version, MAK_Version, MAK_Version, GetTestsByGroup(GROUPS.GROUP3))) TestSets.add(new TestSet("Options_" + GROUPS.GROUP5, MAK_Version, MAK_Version, MAK_Version, GetTestsByGroup(GROUPS.GROUP5))) TestSets.add(new TestSet("Options_" + GROUPS.GROUP6, MAK_Version, MAK_Version, MAK_Version, GetTestsByGroup(GROUPS.GROUP6))) TestSets.add(new TestSet("Options_" + GROUPS.GROUP7, MAK_Version, MAK_Version, MAK_Version, GetTestsByGroup(GROUPS.GROUP7))) TestSets.add(new TestSet("Options_" + GROUPS.GROUP8, MAK_Version, MAK_Version, MAK_Version, GetTestsByGroup(GROUPS.GROUP8))) } else if (!allSelected && selectedGroup.equals(GROUPS.GROUP1) && !SelectedGroups.contains(GROUPS.GROUP1)) { SelectedGroups.add(selectedGroup); TestSets.add(new TestSet("Options_" + selectedGroup, MAK_Version, MAK_Version, MAK_Version, GetTestsByGroup(selectedGroup))) } else if (!allSelected && selectedGroup.equals(GROUPS.GROUP2) && !SelectedGroups.contains(GROUPS.GROUP2)) { SelectedGroups.add(selectedGroup); TestSets.add(new TestSet("Options_" + selectedGroup, MAK_Version, MAK_Version, MAK_Version, GetTestsByGroup(selectedGroup))) } else if (!allSelected && selectedGroup.equals(GROUPS.GROUP3) && !SelectedGroups.contains(GROUPS.GROUP3)) { SelectedGroups.add(selectedGroup); TestSets.add(new TestSet("Options_" + selectedGroup, MAK_Version, MAK_Version, MAK_Version, GetTestsByGroup(selectedGroup))) } else if (!allSelected && selectedGroup.equals(GROUPS.GROUP4) && !SelectedGroups.contains(GROUPS.GROUP4)) { SelectedGroups.add(selectedGroup); TestSets.add(new TestSet("Options_" + selectedGroup, MAK_Version, MAK_Version, MAK_Version, GetTestsByGroup(selectedGroup))) } else if (!allSelected && selectedGroup.equals(GROUPS.GROUP5) && !SelectedGroups.contains(GROUPS.GROUP5)) { SelectedGroups.add(selectedGroup); TestSets.add(new TestSet("Options_" + selectedGroup, MAK_Version, MAK_Version, MAK_Version, GetTestsByGroup(selectedGroup))) } else if (!allSelected && selectedGroup.equals(GROUPS.GROUP6) && !SelectedGroups.contains(GROUPS.GROUP6)) { SelectedGroups.add(selectedGroup); TestSets.add(new TestSet("Options_" + selectedGroup, MAK_Version, MAK_Version, MAK_Version, GetTestsByGroup(selectedGroup))) } else if (!allSelected && selectedGroup.equals(GROUPS.GROUP7) && !SelectedGroups.contains(GROUPS.GROUP7)) { SelectedGroups.add(selectedGroup); TestSets.add(new TestSet("Options_" + selectedGroup, MAK_Version, MAK_Version, MAK_Version, GetTestsByGroup(selectedGroup))) } else if (!allSelected && selectedGroup.equals(GROUPS.GROUP8) && !SelectedGroups.contains(GROUPS.GROUP8)) { SelectedGroups.add(selectedGroup); TestSets.add(new TestSet("Options_" + selectedGroup, MAK_Version, MAK_Version, MAK_Version, GetTestsByGroup(selectedGroup))) } } } } println "SelectedGroups" println "=====================================" SelectedGroups.each() { println "${it}" } println "-----END------" println "SelectedGroup(s) Test(s)" println "=====================================" TestSets.each() { println "GROUP: ${it}" println "TESTS: ${it.Tests}" } println "----------------------------END----------------------------------------" // Log the parameters. println "Build parameters:" println "\tRunID=${RunID}" println "\tProject=${Project}" println "\tEnvironment=${Environment}" println "\tMAK__7_6_0_Build=${MAK__7_6_0_Build}" println "\tTestMachines(JSON)=${JSONTestMachines}" println "\tJSONselectedTestGroups(JSON)=${JSONselectedTestGroups}" LogTestSets(TestSets, ALL, "") LogTestMachines(TestMachines, ALL, AVAILABLE) // Validate the parameters. if (TestSets.size() == 0) { println "Zero test sets have been requested" System.exit(1) } if (TestMachines.size() < 2) { println "Insufficient test machines have been selected (minimum is 3)" System.exit(1) } // Create lists of machines on which we will install the autotestserver and MAK_s. def jobParameter = "" def jobParameterMAK_1= "" def jobParameterMAK_2= "" def atsParameters = [] def atwsParameters = [] def MAK_730Parameters = [] def MAK_740Parameters = [] def MAK_741Parameters = [] def count = 1 TestMachines.each() { jobParameter += (count == 1) ? it.Name : ",${it.Name}" count++ } atsParameters.add("Machines,${jobParameter}") atwsParameters.add("Machines,${jobParameter}") MAK_730Parameters.add("BuildNumber,${MAK__7_6_0_Build}\nDEPLOY_TO,${jobParameter}") MAK_740Parameters.add("BuildNumber,${MAK__7_6_0_Build}\nDEPLOY_TO,${jobParameter}") MAK_741Parameters.add("BuildNumber,${MAK__7_6_0_Build}\nDEPLOY_TO,${jobParameter}") TestSets.each() { jobParameterMAK_1 += (count == 1) ? it.MAK_1Release : ",${it.MAK_1Release}" jobParameterMAK_2 += (count == 1) ? it.MAK_2Release : ",${it.MAK_2Release}" } if(jobParameterMAK_1.contains("7_3") || jobParameterMAK_2.contains("7_3")) { println "Deploy MAK_ release 7.3.0 parameters (${MAK_730Parameters.size()}):" MAK_730Parameters.each() { println "\t[ ${it} ]" } } if(jobParameterMAK_1.contains("7_4") || jobParameterMAK_2.contains("7_4")) { println "Deploy MAK_ release 7.4.0 parameters (${MAK_740Parameters.size()}):" MAK_740Parameters.each() { println "\t[ ${it} ]" } } println "Deploy MAK_ release 7.4.1 parameters (${MAK_741Parameters.size()}):" MAK_741Parameters.each() { println "\t[ ${it.replace("\n", " - ")} ]" } println "" println "" LogTestMachines(TestMachines, !ALL, AVAILABLE) LogTestMachines(TestMachines, !ALL, !AVAILABLE) println "" LogTestSets(TestSets, !ALL, TEST_SET_WAITING) LogTestSets(TestSets, !ALL, TEST_SET_IN_PROGRESS) LogTestSets(TestSets, !ALL, TEST_SET_COMPLETE) // Run jobs when machines are available. while (GetTestSetCount(TestSets, !ALL, TEST_SET_COMPLETE ) < TestSets.size()) { def statusChanged = false for (int i = 0; i < TestSets.size(); i++) { if (TestSets[i].Status.equals(TEST_SET_IN_PROGRESS)) { def jenkins = Jenkins.instance def jobs = jenkins.items.findAll { job -> job.name.equals('MAK_qaRunOptionsThread') } jobs.each { job -> def build = job.getBuildByNumber(TestSets[i].JobID) if (build != null) {   if (!build.isBuilding()) { TestSets[i].Status = TEST_SET_COMPLETE TestMachines[TestSets[i].MachineID1].Available = AVAILABLE TestMachines[TestSets[i].MachineID2].Available = AVAILABLE TestMachines[TestSets[i].MachineID3].Available = AVAILABLE statusChanged = true } } } } } for (int i = 0; i < TestSets.size(); i++) { if (TestSets[i].Status.equals(TEST_SET_WAITING)) { if (GetTestMachineCount(TestMachines, !ALL, AVAILABLE) >= 3) { def machineID1 = ReserveMachine(TestMachines) def machineID2 = ReserveMachine(TestMachines) def machineID3 = ReserveMachine(TestMachines) if (machineID1 >= 0 && machineID2 >= 0 && machineID3 >= 0) { def jobInfo = Jenkins.instance.getItemByFullName('MAK_qaRunOptionsThread') def jobID = jobInfo.getLastBuild().getNumber() + 1 def params = [ new StringParameterValue('Project', build.buildVariableResolver.resolve("Project")), new StringParameterValue('RunID', RunID), new StringParameterValue('TestSet', TestSets[i].Name), new StringParameterValue('MAK_Build', build.buildVariableResolver.resolve("MAK__7_6_0_Build")), new StringParameterValue('MAK_Build2', build.buildVariableResolver.resolve("MAK__7_6_0_Build")), new StringParameterValue('MAK_Build3', build.buildVariableResolver.resolve("MAK__7_6_0_Build")), new StringParameterValue('Environment', build.buildVariableResolver.resolve("Environment")), new StringParameterValue('MachineName1', TestMachines[machineID1].Name), new StringParameterValue('MachineName2', TestMachines[machineID2].Name), new StringParameterValue('MachineName3', TestMachines[machineID3].Name), new StringParameterValue('MAK_1Release', "${TestSets[i].MAK_1Release}"), new StringParameterValue('MAK_2Release', "${TestSets[i].MAK_2Release}"), new StringParameterValue('MAK_3Release', "${TestSets[i].MAK_3Release}"), new StringParameterValue('TestList', TestSets[i].Tests.join(',')), ] job = Hudson.instance.getJob('MAK_qaRunOptionsThread') def execution = job.scheduleBuild2(0, new Cause.UpstreamCause(build), new ParametersAction(params)) TestSets[i].Status = TEST_SET_IN_PROGRESS TestSets[i].JobID = jobID TestSets[i].MachineID1 = machineID1 TestSets[i].MachineID2 = machineID2 TestSets[i].MachineID3 = machineID3 } statusChanged = true break } } } if (statusChanged) { println "" println "" LogTestMachines(TestMachines, !ALL, AVAILABLE) LogTestMachines(TestMachines, !ALL, !AVAILABLE) println "" LogTestSets(TestSets, !ALL, TEST_SET_WAITING) LogTestSets(TestSets, !ALL, TEST_SET_IN_PROGRESS) LogTestSets(TestSets, !ALL, TEST_SET_COMPLETE) } else { println "." } sleep(120000) } println "DONE!" class GROUPS { static final ALL = "All" static final GROUP1 = "Group1" static final GROUP2 = "Group2" static final GROUP3 = "Group3" static final GROUP4 = "Group4" static final GROUP5 = "Group5" static final GROUP6 = "Group6" static final GROUP7 = "Group7" static final GROUP8 = "Group8" } def GetTestsByGroup(groupName) { switch (groupName) { case GROUPS.ALL: def allGroups = GetTestsByGroup(GROUPS.GROUP1); allGroups.addAll(GetTestsByGroup(GROUPS.GROUP2)); allGroups.addAll(GetTestsByGroup(GROUPS.GROUP3)); allGroups.addAll(GetTestsByGroup(GROUPS.GROUP4)); allGroups.addAll(GetTestsByGroup(GROUPS.GROUP5)); allGroups.addAll(GetTestsByGroup(GROUPS.GROUP6)); allGroups.addAll(GetTestsByGroup(GROUPS.GROUP7)); allGroups.addAll(GetTestsByGroup(GROUPS.GROUP8)); return allGroups; break; case GROUPS.GROUP1: return ["Test1","Test2","Test3","Test4"] break; case GROUPS.GROUP2: return ["Test1","Test2","Test3","Test4"] break; case GROUPS.GROUP3: return return ["Test1","Test2","Test3","Test4"] break; case GROUPS.GROUP4: return ["Test1","Test2","Test3","Test4"] break; case GROUPS.GROUP5: return ["Test1","Test2","Test3","Test4"] break; case GROUPS.GROUP6: return ["Test1","Test2","Test3","Test4"] break; case GROUPS.GROUP7: return ["Test1","Test2","Test3","Test4"] break; case GROUPS.GROUP8: return ["Test1","Test2","Test3","Test4"] break; default: return["Unknown TestGroup"]; } } def LogTestSets(sets, all, status) { if (all) { println "Test sets (All = ${GetTestSetCount(sets, all, status)}):" } else { println "Test sets (${status} = ${GetTestSetCount(sets, !ALL, status)}):" } sets.each() { if (all || it.Status.equals(status)) { println "\t${it.toString()}" } } } def GetTestSetCount(sets, all, status) { def count = 0 sets.each() { if (all || it.Status.equals(status)) { count++ } } return count } def ReserveMachine(machines) { for (int i = 0; i < machines.size(); i++) { if (machines[i].Available.equals(AVAILABLE)) { machines[i].Available = !AVAILABLE return i } } return -1 } def LogTestMachines(machines, all, available) { if (all) { println "Test machines (All = ${GetTestMachineCount(machines, ALL, available)}):" } else { def availability = (available) ? "Available" : "Unavailable" println "Test machines (${availability} = ${GetTestMachineCount(machines, !ALL, available)}):" } machines.each() { if (all || it.Available.equals(available)) { println "\t${it.toString()}" } } } def GetTestMachineCount(machines, all, available) { def count = 0 machines.each() { if (all || it.Available.equals(available)) { count++ } } return count } // // Represents a test set instance. // class TestSet { String Name String MAK_1Release String MAK_2Release String MAK_3Release String Status int JobID int MachineID1 int MachineID2 int MachineID3 def Tests = [] TestSet(name, MAK_1Release, MAK_2Release, MAK_3Release, tests) { this.Name = name this.MAK_1Release = MAK_1Release this.MAK_2Release = MAK_2Release this.MAK_3Release = MAK_3Release this.Status = "Waiting" this.JobID = -1 this.MachineID1 = -1 this.MachineID2 = -1 this.MachineID3 = -1 this.Tests = tests } String toString() { return "[ Name=${Name}, MAK_1Release=${MAK_1Release}, MAK_2Release=${MAK_2Release}, MAK_3Release=${MAK_3Release}, Status=${Status}, JobID=${JobID}, MachineID1=${MachineID1}, MachineID2=${MachineID2}, MachineID3=${MachineID3}, TestsCount=${Tests.size()}]" } } // // Represents a test machine instance. // class TestMachine { String Name Boolean Available TestMachine(name, available) { this.Name = name this.Available = available } String toString() { return "[ Name=${Name}, Available=${Available} ]" } 

}

**ThreadJob - MAK_QaRunOptionsThread**

**import hudson.model.***

import jenkins.model.* import java.util.concurrent.* // Get test list. def testList = build.buildVariableResolver.resolve("TestList") println "#################### TEST LIST ############################" println testList; println "########################################################" // // Install the Autotest Service and RAT Server onto the test machines. // println "\n\nInstalling the Autotest Service and RAT Server onto the test machines..." // Get the machine name parameters. def MachineName1 = build.buildVariableResolver.resolve("MachineName1") def MachineName2 = build.buildVariableResolver.resolve("MachineName2") def MachineName3 = build.buildVariableResolver.resolve("MachineName3") println "Machine names:\n\tMachineName1: ${MachineName1}\n\tMachineName2: ${MachineName2}\n\tMachineName3: ${MachineName3}" // Convert the machine names to parameters for the Jenkins job. def MachineNames = "${MachineName1},${MachineName2}" if (!MachineName3.equals("")) { MachineNames = "${MachineNames},${MachineName3}" } def parameters = [ new StringParameterValue('Machines', MachineNames) ] println "Jenkins job arguments:\n\t${parameters}" // Install the Autotest Service. println "Installing the Autotest Service..." def job = Hudson.instance.getJob('somejobfordoingSomeTask1') def run = job.scheduleBuild2(0, new Cause.UpstreamCause(build), new ParametersAction(parameters)) def result = run.get().result.toString() if (result == 'SUCCESS') { println "\tCompleted successfully" } else { println "\tFailed: ${result}" return 1 } // Install the RAT Server. println "Installing the RAT Server..." job = Hudson.instance.getJob('somejobfordoingSomeTask1') run = job.scheduleBuild2(0, new Cause.UpstreamCause(build), new ParametersAction(parameters)) result = run.get().result.toString() if (result == 'SUCCESS') { println "\tCompleted successfully" } else { println "\tFailed: ${result}" return 1 } println "--------------------------------------------------------------------------------" println "" 

**println ""**
