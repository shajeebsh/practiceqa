---
title: "Selenium SpecFlow c#"
date: 2016-03-31 04:44:34 
author: shajeebhameed
layout: page
slug: selenium-specflow-c
status: publish
---

==============FEATURE=================== Scenario: LaunchandLogins112677SS Given I have visited site and I have entered credentials of "uid|pwd","12677","2S","CK" Scenario: LaunchandLogins112677SS Given I have visited site and I have entered credentials of "uid|pwd","16527","SL","CK" Scenario: LaunchandLogins112677SS Given I have visited site and I have entered credentials of "uid|pwd","12677","2S","GN" Scenario: LaunchandLogins112677SS Given I have visited site and I have entered credentials of "uid|pwd","16527","SL","GN" ============STEP DEFENITION======================= [Binding] public class Feature1Steps { private IWebDriver driver; private string url; private static HomePage1 homePage = null; DriverBase setup = DriverBase.Instance; [Given(@"I have visited site and I have entered credentials of ""(.*)"",""(.*)"",""(.*)"",""(.*)""")] public void GivenIHaveVisitedSiteAndIHaveEnteredCredentialsOf(string user, string num, string clsType, string tktype) { #region initialize string date = DateTime.Now.AddDays(1).ToString("dd-MM-yyyy"); // GN, CK var users = user.Split('|'); //string clsType = "3A"; //2A,3A,SL,2S string inputValue = @"https://www.irctc.co.in,xxUserIdxx,xxpwdxxx,xxfromxxx,xxtoxxx,xxnoxx,xxCKxGNxx,xxname1xx,xxagexx,xxxMxFxx,xxxname2xxx,xxxagexxx,xxxMxFxxx,xxccnoxxx,xxexpiryyrxx,xxxyearxxx,xxxCvvxxx,xxNameoncardxx"; #endregion driver = setup.Driver; driver.Manage().Window.Maximize(); homePage = new HomePage1(driver, inputValue).NavigateTo().Login(); homePage.EnterDetails(date).SelectDetails(clsType); homePage.CheckStatusAndBook(); homePage.PutDetails(); homePage.CC(); } } ===============PAGE OBJECT============================ public class HomePage1 { private readonly IWebDriver driver; private readonly string url; private string[] collection = null; #region Url public HomePage1(IWebDriver driver, string inputVals) { this.driver = driver; collection = inputVals.Split(','); url = collection[0]; } #endregion public HomePage1 NavigateTo() { driver.Navigate().GoToUrl(url); return this; } public HomePage1 Login() { driver.FindElement(By.Id("usernameId")).SendKeys(collection[1]); driver.FindElement(By.Name("j_password")).SendKeys(collection[2]); driver.FindElement(By.Name("j_captcha")).SendKeys(string.Empty); return this; } public HomePage1 EnterDetails(string date) { WaitUntil(By.Id("jpform:fromStation"), 15); driver.FindElement(By.Id("jpform:fromStation")).SendKeys(collection[3]);//CRLM driver.FindElement(By.Id("jpform:toStation")).SendKeys(collection[4]);//PGT driver.FindElement(By.Id("jpform:journeyDateInputDate")).SendKeys(date); driver.FindElement(By.Id("jpform:jpsubmit")).Click(); return this; } public HomePage1 SelectDetails(string clsType) { string num = collection[5]; string type = collection[6]; driver.FindElement(By.XPath("//input[@value='" + type + "']")).Click(); //System.Threading.Thread.Sleep(1000); WaitUntil(By.Id("avlAndFareForm:trainbtwnstns"), 15); var table = driver.FindElement(By.Id("avlAndFareForm:trainbtwnstns")); var rows = table.FindElements(By.TagName("tr")); foreach (var row in rows) { var cols = row.FindElements(By.TagName(("td"))); if (cols.Count > 0) { var numberCell = cols[0]; if (numberCell.Text.Equals(num)) { var elem = cols[15].FindElement(By.LinkText(clsType)); Assert.IsTrue(elem.Displayed, "Not Found in Col 15 - Success"); elem.Click(); } } } return this; } public HomePage1 CheckStatusAndBook() { //li[contains(text(),'16527-SL-CK')] //table[@class='t1']//td[contains(text(),'AVAILABLE')] //td[contains(text(),'AVAILABLE')]/a[contains(text(),'Book')] var elems = driver.FindElements(By.XPath("//td[contains(text(),'AVAILABLE')]")); foreach (var elem in elems) { if (elem.Text.Contains("AVAILABLE")) { var bk = elem.FindElement(By.TagName("a")); Assert.IsNotNull(bk, "Object is null"); bk.Click(); break; } } return this; } public HomePage1 PutDetails() { WaitUntil(By.XPath("//input[@class='input-style1 psgn-name']"), 15); var names = driver.FindElements(By.XPath("//input[@class='input-style1 psgn-name']")); names[0].SendKeys(collection[7]); driver.FindElement(By.Id("addPassengerForm:psdetail:0:psgnAge")).SendKeys(collection[8]); var elemM = driver.FindElement(By.Id("addPassengerForm:psdetail:0:psgnGender")); SelectElement sel = new SelectElement(elemM); sel.SelectByValue(collection[9]); names[1].SendKeys(collection[10]); driver.FindElement(By.Id("addPassengerForm:psdetail:1:psgnAge")).SendKeys(collection[11]); var elemF = driver.FindElement(By.Id("addPassengerForm:psdetail:1:psgnGender")); SelectElement sel1 = new SelectElement(elemF); sel1.SelectByValue(collection[12]); driver.FindElement(By.Id("j_captcha")).SendKeys(string.Empty); //driver.FindElement(By.Name("validate")).Click(); return this; } public HomePage1 CC() { WaitUntil(By.Id("PREFERRED"), 30); driver.FindElement(By.Id("PREFERRED")).Click(); var elemT = driver.FindElement(By.Id("card_type_id")); SelectElement sel = new SelectElement(elemT); sel.SelectByValue("MC"); driver.FindElement(By.Id("card_no_id")).SendKeys(collection[13]); var elemMn = driver.FindElement(By.Id("card_expiry_mon_id")); SelectElement selMn = new SelectElement(elemMn); selMn.SelectByValue(collection[14]); driver.FindElement(By.Id("card_expiry_year_id")).SendKeys(collection[15]); driver.FindElement(By.Id("cvv_no_id")).SendKeys(collection[16]); driver.FindElement(By.Id("card_name_id")).SendKeys(collection[17]); driver.FindElement(By.Id("captcha_txt")).SendKeys(""); //driver.FindElement(By.Name("validate")).Click(); return this; } private void WaitUntil(By by, int timeSpanInSeconds) { WebDriverWait wait = new WebDriverWait(driver, TimeSpan.FromSeconds(timeSpanInSeconds)); wait.Until(ExpectedConditions.VisibilityOfAllElementsLocatedBy(by)); } } ============BASE========================= public class DriverBase { private static IWebDriver driver = null; private static volatile bool disposed = false; //Lazy - typically use it when we want to instantiate something the first time its actually used. This delays the cost of creating it till if/when it's needed instead of always incurring the cost. //Usually this is preferable when the object may or may not be used and the cost of constructing it is non-trivial. private static readonly Lazy<DriverBase> instance = new Lazy<DriverBase>(MakeCommonSetup); public static DriverBase Instance { get { var value = instance.Value; disposed = false; return value; } } public IWebDriver Driver { get { if (IsDriverCreated()) { driver.Quit(); } driver = MakeDriverInstance(); return driver; } } private static int DEFAULT_PAGE_LOAD_TIMEOUT_SECONDS = 5; private IWebDriver MakeDriverInstance() { var driver = new FirefoxDriver(); driver.Manage().Timeouts().ImplicitlyWait(TimeSpan.FromSeconds(DEFAULT_PAGE_LOAD_TIMEOUT_SECONDS)); return driver; } private static DriverBase MakeCommonSetup() { var driverBase = new DriverBase(); AppDomain.CurrentDomain.ProcessExit += (sender, e) => DisposeInstance(); return driverBase; } private static void DisposeInstance() { if (IsDriverCreated() && !disposed){ driver.Quit(); disposed = true; } } private static bool IsDriverCreated() { return driver != null; } //~DriverBase() //{ // DisposeInstance(); //} }
