---
title: "Pandas Cheatsheet"
date: 2026-04-26 18:17:23 
author: shajeebhameed
layout: page
slug: pandas-cheatsheet__trashed
status: trash
---

@import url('https://fonts.googleapis.com/css2?family=JetBrains+Mono:wght@400;600;700&family=Syne:wght@400;600;700;800&display=swap'); :root { \--bg: #0a0e1a; \--surface: #111827; \--surface2: #1a2235; \--border: #1e2d45; \--accent1: #00d4ff; \--accent2: #ff6b35; \--accent3: #a8ff3e; \--accent4: #c084fc; \--accent5: #fb7185; \--text: #e2e8f0; \--text2: #94a3b8; \--text3: #64748b; \--code-bg: #0d1117; \--radius: 6px; } * { box-sizing: border-box; margin: 0; padding: 0; } body { background: var(--bg); color: var(--text); font-family: 'Syne', sans-serif; font-size: 13px; line-height: 1.5; padding: 0; } /* ── HEADER ── */ .header { background: linear-gradient(135deg, #0a0e1a 0%, #111827 50%, #0a1628 100%); border-bottom: 1px solid var(--border); padding: 20px 24px 16px; position: sticky; top: 0; z-index: 100; backdrop-filter: blur(12px); } .header-top { display: flex; align-items: center; justify-content: space-between; margin-bottom: 14px; } .logo { display: flex; align-items: center; gap: 10px; } .logo-icon { width: 32px; height: 32px; border-radius: 8px; background: linear-gradient(135deg, var(--accent1), var(--accent4)); display: flex; align-items: center; justify-content: center; font-size: 16px; font-weight: 800; color: #000; font-family: 'JetBrains Mono', monospace; } .logo-text { font-size: 18px; font-weight: 800; color: var(--text); letter-spacing: -0.5px; } .logo-sub { font-size: 11px; color: var(--text3); font-family: 'JetBrains Mono', monospace; margin-top: 1px; } .badge { font-size: 10px; font-family: 'JetBrains Mono', monospace; padding: 3px 8px; border-radius: 20px; font-weight: 600; background: rgba(0,212,255,0.1); color: var(--accent1); border: 1px solid rgba(0,212,255,0.2); } /* ── NAV TABS ── */ .nav { display: flex; gap: 4px; overflow-x: auto; padding-bottom: 2px; scrollbar-width: none; } .nav::-webkit-scrollbar { display: none; } .nav-btn { padding: 6px 14px; border-radius: 20px; border: 1px solid var(--border); background: transparent; color: var(--text3); font-family: 'Syne', sans-serif; font-size: 12px; font-weight: 600; cursor: pointer; white-space: nowrap; transition: all 0.15s; letter-spacing: 0.02em; } .nav-btn:hover { background: var(--surface2); color: var(--text); border-color: var(--border); } .nav-btn.active { background: var(--accent1); color: #000; border-color: var(--accent1); } /* ── SEARCH ── */ .search-wrap { padding: 12px 24px 0; } .search { width: 100%; padding: 8px 14px; border-radius: var(--radius); background: var(--surface); border: 1px solid var(--border); color: var(--text); font-family: 'JetBrains Mono', monospace; font-size: 12px; outline: none; transition: border-color 0.15s; } .search:focus { border-color: var(--accent1); } .search::placeholder { color: var(--text3); } /* ── CONTENT ── */ .content { padding: 16px 24px 40px; } /* ── SECTION ── */ .section { margin-bottom: 28px; } .section-label { font-size: 10px; font-weight: 700; color: var(--text3); text-transform: uppercase; letter-spacing: 0.1em; margin-bottom: 10px; display: flex; align-items: center; gap: 8px; } .section-label::after { content: ''; flex: 1; height: 1px; background: var(--border); } /* ── CARD GRID ── */ .grid { display: grid; grid-template-columns: repeat(auto-fill, minmax(320px, 1fr)); gap: 10px; } .grid-2 { grid-template-columns: repeat(auto-fill, minmax(280px, 1fr)); } /* ── CARD ── */ .card { background: var(--surface); border: 1px solid var(--border); border-radius: var(--radius); overflow: hidden; transition: border-color 0.15s, transform 0.1s; cursor: pointer; } .card:hover { border-color: var(--accent1); transform: translateY(-1px); } .card.open { border-color: rgba(0,212,255,0.4); } .card-head { padding: 10px 12px; display: flex; align-items: flex-start; gap: 10px; user-select: none; } .cat-dot { width: 8px; height: 8px; border-radius: 50%; flex-shrink: 0; margin-top: 5px; } .card-title { font-size: 13px; font-weight: 600; color: var(--text); flex: 1; line-height: 1.35; } .card-sub { font-size: 11px; color: var(--text3); margin-top: 2px; } .chev { color: var(--text3); font-size: 10px; flex-shrink: 0; margin-top: 4px; transition: transform 0.2s; } .chev.open { transform: rotate(180deg); } .card-body { display: none; border-top: 1px solid var(--border); } .card-body.open { display: block; } /* ── CODE BLOCK ── */ .code-block { background: var(--code-bg); padding: 10px 12px; font-family: 'JetBrains Mono', monospace; font-size: 11.5px; color: #c9d1d9; line-height: 1.65; overflow-x: auto; white-space: pre; } .kw { color: #ff79c6; } /* keyword */ .fn { color: #50fa7b; } /* function */ .st { color: #f1fa8c; } /* string */ .cm { color: #6272a4; } /* comment */ .nm { color: #bd93f9; } /* number */ .cl { color: #8be9fd; } /* class/type */ /* ── INSIGHT STRIP ── */ .insight { background: rgba(0,212,255,0.04); border-left: 2px solid var(--accent1); padding: 8px 12px; font-size: 12px; color: var(--text2); line-height: 1.6; } .insight b { color: var(--accent1); font-weight: 600; } /* ── OUTPUT STRIP ── */ .output { background: rgba(168,255,62,0.04); border-left: 2px solid var(--accent3); padding: 8px 12px; font-family: 'JetBrains Mono', monospace; font-size: 11px; color: #94a3b8; line-height: 1.65; white-space: pre-wrap; } .output-lbl { font-size: 10px; font-weight: 700; color: var(--accent3); text-transform: uppercase; letter-spacing: 0.08em; padding: 5px 12px; background: rgba(168,255,62,0.06); border-bottom: 1px solid var(--border); } /* ── SQL COMPARE ── */ .compare { display: grid; grid-template-columns: 1fr 1fr; } .compare-col { } .compare-col:first-child { border-right: 1px solid var(--border); } .compare-lbl { font-size: 10px; font-weight: 700; padding: 5px 12px; text-transform: uppercase; letter-spacing: 0.08em; border-bottom: 1px solid var(--border); } .sql-lbl { color: var(--accent2); background: rgba(255,107,53,0.06); } .pd-lbl { color: var(--accent1); background: rgba(0,212,255,0.06); } /* ── QUICK REF TABLE ── */ .ref-table { width: 100%; border-collapse: collapse; } .ref-table th { text-align: left; padding: 7px 12px; font-size: 10px; font-weight: 700; color: var(--text3); text-transform: uppercase; letter-spacing: 0.08em; border-bottom: 1px solid var(--border); background: var(--code-bg); } .ref-table td { padding: 7px 12px; border-bottom: 1px solid var(--border); font-size: 12px; vertical-align: top; } .ref-table tr:last-child td { border-bottom: none; } .ref-table tr:hover td { background: var(--surface2); } .mono { font-family: 'JetBrains Mono', monospace; font-size: 11px; color: var(--accent1); } .tag { display: inline-block; font-size: 10px; font-family: 'JetBrains Mono', monospace; padding: 1px 6px; border-radius: 3px; font-weight: 600; margin-right: 3px; } .tag-wf { background: rgba(192,132,252,0.15); color: var(--accent4); } .tag-dq { background: rgba(0,212,255,0.15); color: var(--accent1); } .tag-grp { background: rgba(168,255,62,0.15); color: var(--accent3); } .tag-io { background: rgba(255,107,53,0.15); color: var(--accent2); } .tag-out { background: rgba(251,113,133,0.15); color: var(--accent5); } /* ── EMPTY STATE ── */ .empty { text-align: center; padding: 40px; color: var(--text3); } /* ── PILL COUNT ── */ .count-pill { display: inline-flex; align-items: center; justify-content: center; min-width: 18px; height: 18px; padding: 0 5px; border-radius: 9px; font-size: 10px; font-weight: 700; background: rgba(0,212,255,0.15); color: var(--accent1); font-family: 'JetBrains Mono', monospace; margin-left: 6px; } 

pd

Pandas Cheat Sheet

Insurance Claims · Data Quality · Window Functions

EXL Assessment Ready

const CAT_COLORS = { 'Loading': '#00d4ff', 'Inspection': '#a8ff3e', 'Selection': '#ff6b35', 'Cleaning': '#c084fc', 'GroupBy': '#fb7185', 'Window': '#f59e0b', 'Outliers': '#34d399', 'Merge/Join': '#60a5fa', 'DateTime': '#e879f9', 'String': '#f97316', 'Export/IO': '#94a3b8', 'SQL Mapping': '#00d4ff', 'DQ Dimensions': '#a8ff3e', 'Insurance': '#fb7185', }; const CARDS = [ // ── LOADING & INSPECTION ────────────────────────────────────────────────── { cat:'Loading', title:'Read CSV / Excel / JSON', sub:'Import data from files', code:`# CSV — most common df = pd.read_csv('claims.csv') df = pd.read_csv('claims.csv', parse_dates=['fnol_date']) # Excel df = pd.read_excel('claims.xlsx', sheet_name='Sheet1') # JSON df = pd.read_json('claims.json') # From SQL (SQLite example) import sqlite3 conn = sqlite3.connect(':memory:') df = pd.read_sql('SELECT * FROM claims', conn)`, insight:'Always pass parse_dates when loading — avoids silent string comparisons on date columns later.', output:` claim_id claimant_id claim_type claim_amount fnol_date 0 CLM0001 CUST039 Liability 894.39 2023-11-10 1 CLM0002 CUST052 Liability 17907.33 2026-08-15` }, { cat:'Inspection', title:'df.shape, dtypes, info()', sub:'First thing to run on any dataset', code:`df.shape # (rows, cols) df.dtypes # data type of each column df.info() # types + null counts in one call df.columns.tolist() # list of column names df.head(5) # first 5 rows df.tail(5) # last 5 rows df.sample(5) # 5 random rows`, insight:'Run df.info() before any analysis — it shows dtypes and null counts in one shot. If claim_amount is object instead of float64, your aggregations will silently fail.', output:`(39, 17) RangeIndex: 39 entries claim_id 39 non-null object claim_amount 37 non-null float64 ← 2 nulls visible here fnol_date 39 non-null datetime64` }, { cat:'Inspection', title:'df.describe()', sub:'Distribution profile — always include percentiles', code:`# Default (numeric cols only) df.describe() # Extended with insurance-relevant percentiles df['claim_amount'].describe( percentiles=[.25, .50, .75, .95, .99] ).round(2) # Include categorical columns df.describe(include='all')`, insight:'When mean >> median the data is right-skewed (normal for insurance claims). Always report both in assessments — never just mean.', output:`count 37.00 mean 30241.00 ← inflated by outliers median 10857.00 ← more representative p95 250000.00 p99 380000.00 max 380000.00` }, { cat:'Inspection', title:'value_counts()', sub:'Frequency table for categorical columns', code:`# Absolute count df['claim_type'].value_counts() # With percentage df['claim_type'].value_counts(normalize=True).mul(100).round(1) # Include NaN in count df['provider_id'].value_counts(dropna=False) # Top 3 only df['claim_type'].value_counts().head(3)`, insight:'Use dropna=False to see null counts inline — essential for DQ completeness checks without a separate isnull() call.', output:`Motor 13 (33.3%) Property 8 (20.5%) Liability 8 (20.5%) Health 7 (17.9%) Travel 3 (7.7%)` }, // ── SELECTION & FILTERING ──────────────────────────────────────────────── { cat:'Selection', title:'loc, iloc, column selection', sub:'Row and column access patterns', code:`# Select columns df['claim_amount'] # Series df[['claim_id', 'claim_amount']] # DataFrame # loc: label-based (use column names & index labels) df.loc[0:4, 'claim_id':'claim_type'] # iloc: integer position df.iloc[0:5, 0:4] # Boolean filter df[df['claim_status'] == 'Open'] df[df['claim_amount'] > 10000] # Multiple conditions — use & | ~ (NOT 'and' 'or') df[(df['claim_type'] == 'Motor') & (df['claim_amount'] > 5000)] df[(df['region'] == 'Dublin') | (df['region'] == 'Cork')]`, insight:'Always wrap each condition in parentheses when using & or | — operator precedence causes silent bugs if you forget.', }, { cat:'Selection', title:'isin(), between(), query()', sub:'Cleaner filtering for multiple values', code:`# isin — equivalent of SQL IN () df[df['region'].isin(['Dublin', 'Cork', 'Galway'])] # between — inclusive on both ends df[df['claim_amount'].between(1000, 50000)] # query — SQL-like string syntax (great for readability) df.query("claim_type == 'Motor' and claim_amount > 5000") df.query("region in ['Dublin','Cork'] and fraud_flag == 1") # NOT isin df[~df['claim_status'].isin(['Closed', 'Cancelled'])]`, insight:'query() is the most readable for complex filters — looks almost identical to SQL WHERE clauses, which helps when translating between SQL and Pandas.', }, // ── CLEANING ──────────────────────────────────────────────────────────── { cat:'Cleaning', title:'isnull() and fillna()', sub:'Null detection and imputation', code:`# Count nulls per column df.isnull().sum() # Null percentage (df.isnull().sum() / len(df) * 100).round(2) # Filter rows where specific column is null df[df['claim_amount'].isnull()] df[df['claim_amount'].notna()] # opposite # Fill with a constant df['claim_amount'].fillna(0) # Fill with median (safer for skewed insurance data) df['claim_amount'].fillna(df['claim_amount'].median()) # Forward fill (carry last known value) df['reserve_amount'].fillna(method='ffill')`, insight:'For insurance claim amounts, fill nulls with median not mean. The mean is inflated by large claims and misrepresents typical values.', }, { cat:'Cleaning', title:'drop_duplicates()', sub:'Remove duplicate rows', code:`# Drop exact duplicate rows df.drop_duplicates() # Drop duplicates by specific columns (keep last = most recent load) df.drop_duplicates(subset='claim_id', keep='last') # Keep first occurrence df.drop_duplicates(subset='claim_id', keep='first') # Find duplicates (flag them, don't remove) df[df.duplicated(subset='claim_id', keep=False)] # Count of duplicates df.duplicated(subset='claim_id').sum()`, insight:'SQL equivalent: ROW_NUMBER() OVER (PARTITION BY claim_id ORDER BY fnol_date DESC) — keep rn=1. In Pandas, sort before drop_duplicates to control which record is kept.', }, { cat:'Cleaning', title:'astype() and type conversion', sub:'Fix column data types', code:`# Convert to numeric (errors='coerce' turns bad values to NaN) df['claim_amount'] = pd.to_numeric(df['claim_amount'], errors='coerce') # Convert to datetime df['fnol_date'] = pd.to_datetime(df['fnol_date'], errors='coerce') # Cast to specific type df['fraud_flag'] = df['fraud_flag'].astype(int) df['claim_id'] = df['claim_id'].astype(str) # Convert bool to int (for aggregation) mask = df['claim_amount'] > 10000 mask.astype(int) # True=1, False=0 → can now SUM()`, insight:'errors="coerce" is your safety net — bad values become NaN instead of crashing the pipeline. Always use it when reading from external sources.', }, { cat:'Cleaning', title:'rename(), drop(), assign()', sub:'Restructure columns', code:`# Rename columns df.rename(columns={'claim_amount': 'gross_claim', 'paid_amount': 'net_paid'}) # Drop columns df.drop(columns=['policy_id', 'loss_date']) # Drop rows by index df.drop(index=[0, 5, 10]) # Add a new calculated column (assign is chainable) df = df.assign( loss_ratio=lambda x: (x['paid_amount'] / x['premium'] * 100).round(1), days_open=lambda x: (pd.Timestamp('2025-04-24') - x['fnol_date']).dt.days )`, insight:'assign() is chainable — you can add multiple columns in one call, and later columns can reference earlier ones in the same assign() call.', }, // ── GROUPBY ────────────────────────────────────────────────────────────── { cat:'GroupBy', title:'groupby() + agg()', sub:'Aggregate by one or more columns', code:`# Simple aggregation df.groupby('claim_type')['claim_amount'].mean() # Multiple aggregations — named columns df.groupby('claim_type')['claim_amount'].agg( count='count', mean='mean', median='median', total='sum', p95=lambda x: x.quantile(.95) ).round(2) # Multi-column groupby df.groupby(['claim_type', 'region']).agg( claims=('claim_id', 'count'), avg_amount=('claim_amount', 'mean'), fraud_rate=('fraud_flag', 'mean') ).reset_index()`, insight:'reset_index() converts the groupby index back to regular columns — almost always needed when using the result downstream.', }, { cat:'GroupBy', title:'groupby() + transform()', sub:'Add group statistic back to original rows', code:`# Add group mean back to each row (SQL: AVG() OVER (PARTITION BY)) df['type_avg_claim'] = df.groupby('claim_type')['claim_amount'].transform('mean') # Deviation from group mean df['vs_type_avg'] = df['claim_amount'] - df['type_avg_claim'] # Group count back to rows df['claims_in_type'] = df.groupby('claim_type')['claim_id'].transform('count') # Flag above-average claims within type df['above_avg_in_type'] = ( df['claim_amount'] > df.groupby('claim_type')['claim_amount'].transform('mean') ).astype(int)`, insight:'transform() is the key difference from agg() — it returns a Series with the same index as the original DataFrame, so you can add the group stat as a new column without merging.', }, { cat:'GroupBy', title:'pivot_table()', sub:'Cross-dimensional analysis — region × claim type', code:`# Loss ratio heatmap: region × claim type pivot = df.pivot_table( index='region', columns='claim_type', values='paid_amount', aggfunc='sum' ).round(0) # Fraud rate by adjuster × claim type df.pivot_table( index='adjuster_id', columns='claim_type', values='fraud_flag', aggfunc='mean' ).round(3) # With margins (row/column totals) df.pivot_table( index='region', columns='claim_status', values='claim_id', aggfunc='count', margins=True )`, insight:'The cross-dimensional view from pivot_table is where real insights live. A 75% loss ratio looks fine overall but pivot_table reveals Dublin Motor at 110%. That is the finding that drives a pricing recommendation.', }, // ── WINDOW FUNCTIONS ────────────────────────────────────────────────────── { cat:'Window', title:'shift() — LAG and LEAD', sub:'Look back or forward within a group', code:`# Sort first — always required before shift() df = df.sort_values(['claimant_id', 'fnol_date']) # LAG(1) — previous row's value within group df['prev_fnol'] = df.groupby('claimant_id')['fnol_date'].shift(1) df['prev_amount'] = df.groupby('claimant_id')['claim_amount'].shift(1) # LEAD(1) — next row's value within group df['next_fnol'] = df.groupby('claimant_id')['fnol_date'].shift(-1) # Days since last claim (fraud detection) df['days_since_last'] = (df['fnol_date'] - df['prev_fnol']).dt.days # Flag repeat claims within 30 days df['repeat_flag'] = (df['days_since_last'] <= 30).astype(int)`, insight:'shift(1) = LAG(1) | shift(-1) = LEAD(1) | shift(n) = LAG(n). Always sort by the partition key + order key before shift — the results are position-based, not value-based.', }, { cat:'Window', title:'rank() — ROW_NUMBER, RANK, DENSE_RANK', sub:'Rank rows within groups', code:`# ROW_NUMBER — unique, no ties df['row_num'] = df.groupby('claimant_id')['claim_amount'].rank( method='first', ascending=False) # RANK — ties get same rank, gaps after df['rank'] = df['claim_amount'].rank(method='min', ascending=False) # DENSE_RANK — ties same rank, no gaps df['dense_rank'] = df['claim_amount'].rank(method='dense', ascending=False) # PERCENT_RANK — 0 to 1 (multiply by 100 for percentile) df['pct_rank'] = df.groupby('claim_type')['claim_amount'].rank(pct=True) # Top claim per claimant (ROW_NUMBER + filter = 1) top = (df.sort_values('claim_amount', ascending=False) .groupby('claimant_id').first().reset_index())`, insight:"rank(method='first') = ROW_NUMBER | rank(method='min') = RANK | rank(method='dense') = DENSE_RANK | rank(pct=True) = PERCENT_RANK. The method parameter is what differentiates them.", }, { cat:'Window', title:'cumsum() and cumcount() — running totals', sub:'Cumulative calculations', code:`# Running total (SQL: SUM OVER UNBOUNDED PRECEDING) df['running_total'] = df['claim_amount'].cumsum() # Claim sequence per claimant (SQL: ROW_NUMBER) df['claim_seq'] = df.groupby('claimant_id').cumcount() + 1 # Running total within group (partitioned) df['running_by_type'] = df.groupby('claim_type')['claim_amount'].cumsum() # Running fraud count per claimant df['running_fraud'] = df.groupby('claimant_id')['fraud_flag'].cumsum()`, insight:'cumcount() + 1 is the cleanest ROW_NUMBER equivalent. It starts at 0 by default so always add +1 for 1-based numbering.', }, { cat:'Window', title:'rolling() — moving averages and windows', sub:'ROWS BETWEEN in SQL', code:`# 3-month rolling average (SQL: ROWS BETWEEN 2 PRECEDING AND CURRENT ROW) df['rolling_3mo'] = df['monthly_avg'].rolling(3).mean() # 12-month rolling (SQL: ROWS BETWEEN 11 PRECEDING AND CURRENT ROW) df['rolling_12mo'] = df['monthly_avg'].rolling(12).mean() # min_periods=1 avoids NaN for first rows df['rolling_3mo'] = df['monthly_avg'].rolling(3, min_periods=1).mean() # Rolling std dev (detect volatility) df['rolling_std'] = df['claim_amount'].rolling(3).std() # Rolling max (peak value in window) df['rolling_max'] = df['claim_amount'].rolling(3).max()`, insight:"rolling(n) = ROWS BETWEEN (n-1) PRECEDING AND CURRENT ROW. The rule: rolling(3) covers current + 2 previous = 3 rows total. min_periods=1 is essential — without it the first n-1 rows produce NaN.", }, { cat:'Window', title:'pd.qcut() and pd.cut() — NTILE banding', sub:'Segment into risk bands', code:`# pd.qcut = equal-frequency (SQL: NTILE) # Each bin has same number of rows df['risk_band'] = pd.qcut( df['claim_amount'], q=4, labels=['Low', 'Medium-Low', 'Medium-High', 'High'] ) # pd.cut = equal-width (NOT NTILE — different concept!) # Each bin covers same value range df['amount_band'] = pd.cut( df['claim_amount'], bins=[0, 5000, 20000, 100000, float('inf')], labels=['Minor', 'Standard', 'Large', 'Major'] ) # Aggregate by risk band df.groupby('risk_band')[['claim_amount', 'fraud_flag']].agg( ['mean', 'sum', 'count'])`, insight:'pd.qcut(q=4) = NTILE(4) — equal frequency, each band has same row count. pd.cut = equal width, each band covers same value range. Use qcut for risk banding, cut for defined business thresholds.', }, // ── OUTLIERS ────────────────────────────────────────────────────────────── { cat:'Outliers', title:'IQR method — recommended for skewed data', sub:'Insurance claims are right-skewed', code:`# Step 1: compute bounds Q1 = df['claim_amount'].quantile(0.25) Q3 = df['claim_amount'].quantile(0.75) IQR = Q3 - Q1 lower = Q1 - 1.5 * IQR upper = Q3 + 1.5 * IQR # Step 2: detect outliers = df[(df['claim_amount'] < lower) | (df['claim_amount'] > upper)] # Step 3: flag (best for insurance — preserve data) df['outlier_flag'] = ( (df['claim_amount'] > upper) | (df['claim_amount'] < lower) ).astype(int) # Per claim_type IQR (more accurate) def iqr_flag(group): q1, q3 = group.quantile([.25, .75]) iqr = q3 - q1 return ((group > q3 + 1.5*iqr) | (group < q1 - 1.5*iqr)).astype(int) df['outlier_by_type'] = df.groupby('claim_type')['claim_amount'].transform(iqr_flag)`, insight:"IQR is anchored to Q1 and Q3 which outliers cannot distort. Z-score uses mean and std dev which the outliers inflate by 3x — making z-score blind to moderate outliers. For right-skewed insurance data, IQR is always the better choice.", }, { cat:'Outliers', title:'Z-score method', sub:'Use for normally distributed data', code:`from scipy import stats as scipy_stats import numpy as np # Compute z-scores df['z_score'] = np.abs(scipy_stats.zscore( df['claim_amount'].dropna() )) # Flag z > 3 outliers_z = df[df['z_score'] > 3] # Manual z-score (no scipy needed) mean = df['claim_amount'].mean() std = df['claim_amount'].std() df['z_manual'] = (df['claim_amount'] - mean).abs() / std`, insight:'Z-score assumes normality. For insurance claims (right-skewed), the outliers inflate std dev by 3x and z-score becomes unreliable. Use IQR for claims. Use z-score for cycle time or days-to-report which are closer to normal.', }, { cat:'Outliers', title:'Winsorising / capping', sub:'Cap outliers at fence — preserve row count', code:`# Winsorise at IQR fences df['claim_capped'] = df['claim_amount'].clip(lower=lower, upper=upper) # Cap at 1st and 99th percentile p1 = df['claim_amount'].quantile(0.01) p99 = df['claim_amount'].quantile(0.99) df['claim_winsorised'] = df['claim_amount'].clip(lower=p1, upper=p99) # KS test — detect data drift between two periods early = df[df['fnol_date'] < '2024-01-01']['claim_amount'].dropna() recent = df[df['fnol_date'] >= '2024-01-01']['claim_amount'].dropna() stat, pval = scipy_stats.ks_2samp(early, recent) print(f"Drift detected: {pval < 0.05}")`, insight:'clip() is Pandas equivalent of SQL CASE WHEN x > upper THEN upper ELSE x END. Winsorising is preferred over removing in insurance — a £380k claim is unusual but legitimate and its removal would distort loss ratios.', }, // ── MERGE / JOIN ────────────────────────────────────────────────────────── { cat:'Merge/Join', title:'merge() — all join types', sub:'SQL JOIN equivalent', code:`# INNER JOIN (keep only matching rows) pd.merge(claims, policies, on='policy_id', how='inner') # LEFT JOIN (keep all claims, add policy info) pd.merge(claims, policies, on='policy_id', how='left') # Different column names pd.merge(claims, adjusters, left_on='adjuster_id', right_on='adj_id', how='left') # Multiple keys pd.merge(df1, df2, on=['claim_id', 'claimant_id'], how='inner') # Check for unmatched rows after LEFT JOIN result = pd.merge(claims, policies, on='policy_id', how='left', indicator=True) unmatched = result[result['_merge'] == 'left_only']`, insight:'indicator=True adds a _merge column showing left_only / right_only / both — essential for DQ referential integrity checks. Unmatched claims (left_only) mean orphaned claims with no policy record.', }, // ── DATETIME ────────────────────────────────────────────────────────────── { cat:'DateTime', title:'Date arithmetic and extraction', sub:'Claims cycle time and SLA', code:`# Convert to datetime df['fnol_date'] = pd.to_datetime(df['fnol_date']) # Extract components df['year'] = df['fnol_date'].dt.year df['month'] = df['fnol_date'].dt.month df['quarter']= df['fnol_date'].dt.quarter df['weekday']= df['fnol_date'].dt.day_name() # Date difference in days (cycle time) df['cycle_days'] = (df['closure_date'] - df['fnol_date']).dt.days # Period conversion (for monthly groupby) df['month_period'] = df['fnol_date'].dt.to_period('M') # Days since today df['days_open'] = (pd.Timestamp('2025-04-24') - df['fnol_date']).dt.days`, insight:'.dt.to_period("M") is the cleanest way to group by month — it preserves sort order and displays as 2023-01 format. Using .dt.month alone loses the year context.', }, // ── DQ DIMENSIONS ──────────────────────────────────────────────────────── { cat:'DQ Dimensions', title:'All 6 DQ dimensions — one-liner pattern', sub:'Completeness · Uniqueness · Validity · Consistency · Timeliness · Accuracy', code:`total = len(df) # 1. COMPLETENESS — null rate completeness = df['claim_amount'].notna().sum() / total * 100 # 2. UNIQUENESS — duplicate rate uniqueness = df['claim_id'].nunique() / total * 100 # 3. VALIDITY — business rules validity = (df['claim_amount'] > 0).sum() / df['claim_amount'].notna().sum() * 100 # 4. CONSISTENCY — status vs date consistency = ((df['claim_status'] != 'Closed') | df['closure_date'].notna()).mean() * 100 # 5. TIMELINESS — SLA compliance open_c = df[df['claim_status']=='Open'].copy() open_c['days_open'] = (pd.Timestamp('today') - open_c['fnol_date']).dt.days timeliness = (open_c['days_open'] <= 90).mean() * 100 # 6. ACCURACY — mean vs median skew ratio clean = df[df['claim_amount'] > 0] skew = clean['claim_amount'].mean() / clean['claim_amount'].median()`, insight:"When mean/median ratio > 2, the data is significantly right-skewed. Insurance claims typically show ratios of 2.5-4x — this is normal and expected. Always mention this ratio when presenting claims analysis.", }, // ── SQL MAPPING ──────────────────────────────────────────────────────────── { cat:'SQL Mapping', title:'SQL → Pandas complete mapping', sub:'Every window function equivalent', table: true, rows: [ ['SELECT *','df','All columns'], ['WHERE x > 10','df[df["x"] > 10]','Boolean filter'], ['WHERE x IN (a,b)','df[df["x"].isin([a,b])]','isin()'], ['GROUP BY + COUNT','df.groupby("x").size()','size() or count()'], ['GROUP BY + AVG','df.groupby("x")["y"].mean()','mean() after groupby'], ['ORDER BY x DESC','df.sort_values("x", ascending=False)','sort_values()'], ['LIMIT 10','df.head(10)','head()'], ['COUNT(DISTINCT x)','df["x"].nunique()','nunique()'], ['SUM(CASE WHEN) / COUNT','mask.astype(int).sum() / len(df)','Boolean → int → sum'], ['DATEDIFF(day, a, b)','(b - a).dt.days','dt.days after subtraction'], ['ROW_NUMBER() OVER (PARTITION BY a ORDER BY b)','sort then groupby().cumcount()+1','cumcount()'], ['RANK() OVER (ORDER BY x)','rank(method="min")','rank method="min"'], ['DENSE_RANK() OVER (ORDER BY x)','rank(method="dense")','rank method="dense"'], ['LAG(x,1) OVER (PARTITION BY a ORDER BY b)','groupby(a)[x].shift(1)','shift(1)'], ['LEAD(x,1) OVER (PARTITION BY a ORDER BY b)','groupby(a)[x].shift(-1)','shift(-1)'], ['SUM(x) OVER (... UNBOUNDED PRECEDING)','cumsum()','cumsum()'], ['AVG(x) OVER (ROWS BETWEEN 2 PRECEDING ...)','rolling(3).mean()','rolling(n)'], ['NTILE(4) OVER (ORDER BY x)','pd.qcut(x, q=4)','pd.qcut — equal freq'], ['PERCENT_RANK() OVER (PARTITION BY a ORDER BY x)','groupby(a)[x].rank(pct=True)','rank(pct=True)'], ['FIRST_VALUE(x) OVER (PARTITION BY a ...)','groupby(a)[x].transform("first")','transform("first")'], ['LAST_VALUE(x) OVER (PARTITION BY a ... UNBOUNDED FOLLOWING)','groupby(a)[x].transform("last")','transform("last") — NO frame gotcha'], ['JOIN ... ON','pd.merge(df1,df2,on=key,how="inner/left")','merge()'], ['LEFT JOIN + unmatched','merge(..., indicator=True) → left_only','indicator=True'], ['UNION ALL','pd.concat([df1,df2])','concat()'], ['PIVOT','df.pivot_table(index, columns, values)','pivot_table()'], ] }, // ── INSURANCE ────────────────────────────────────────────────────────────── { cat:'Insurance', title:'Loss ratio calculation', sub:'Core insurance profitability metric', code:`# Formula: paid_amount / premium × 100 # 100% = unprofitable lr = df.groupby('claim_type').apply(lambda g: pd.Series({ 'claims': len(g), 'total_paid': round(g['paid_amount'].sum(), 2), 'total_premium': round(g['premium'].sum(), 2), 'loss_ratio': round(g['paid_amount'].sum() / g['premium'].sum() * 100, 1) })).reset_index() lr['status'] = lr['loss_ratio'].apply( lambda x: 'Unprofitable' if x > 100 else ('Watch' if x > 70 else 'Healthy')) # Pivot: loss ratio heatmap region × claim type df.pivot_table(index='region', columns='claim_type', values='paid_amount', aggfunc='sum')`, insight:'The portfolio average hides regional concentration. A 75% overall loss ratio can mask Dublin Motor at 110% (unprofitable) subsidised by Waterford Motor at 45%. Always decompose by two dimensions.', }, { cat:'Insurance', title:'Fraud scoring with multiple window methods', sub:'LAG + PERCENT_RANK + cumcount combined', code:`scored = df[df['claim_amount'] > 0].sort_values(['claimant_id','fnol_date']).copy() # LAG: days since last claim scored['prev_fnol'] = scored.groupby('claimant_id')['fnol_date'].shift(1) scored['days_since_last'] = (scored['fnol_date'] - scored['prev_fnol']).dt.days # PERCENT_RANK within type scored['pct_rank'] = scored.groupby('claim_type')['claim_amount'].rank(pct=True).mul(100) # Claim sequence (ROW_NUMBER) scored['claim_seq'] = scored.groupby('claimant_id').cumcount() + 1 # Composite fraud risk score scored['risk_score'] = ( (scored['pct_rank'] >= 95).astype(int) * 3 # top 5% amount \+ (scored['days_since_last'].fillna(999) <= 30).astype(int) * 3 \+ scored['fraud_flag'] * 2 \+ (scored['num_prev_claims'] > 4).astype(int) * 1 )`, insight:'Combining window functions with a composite score is the pattern EXL uses in production fraud models. The score is transparent and auditable — each component has a named business reason, which regulators and clients require.', }, ]; // ── RENDER ──────────────────────────────────────────────────────────────── const CATS = ['All', ...Object.keys(CAT_COLORS)]; let activeTab = 'All'; let openCards = {}; let searchTerm = ''; function renderNav(){ const nav = document.getElementById('nav'); nav.innerHTML = CATS.map(c => { const count = c === 'All' ? CARDS.length : CARDS.filter(x=>x.cat===c).length; return ` ${c}${count} `; }).join(''); } function renderContent(){ const content = document.getElementById('content'); const filtered = CARDS.filter(card => { const matchTab = activeTab === 'All' || card.cat === activeTab; const matchSearch = !searchTerm || [card.title, card.sub, card.code||'', (card.rows||[]).flat().join(' ')] .join(' ').toLowerCase().includes(searchTerm.toLowerCase()); return matchTab && matchSearch; }); if (!filtered.length) { content.innerHTML = '

No results found — try a different search term

'; return; } // Group by category const groups = {}; filtered.forEach(c => { groups[c.cat] = groups[c.cat] || []; groups[c.cat].push(c); }); content.innerHTML = Object.entries(groups).map(([cat, cards]) => ` 

${cat}

${cards.map((card,i) => renderCard(card, cat+i)).join('')}

`).join(''); } function renderCard(card, key){ const isOpen = openCards[key]; const col = CAT_COLORS[card.cat] || '#94a3b8'; if(card.table){ return `

${card.title}

${card.sub}

▼

SQL| Pandas| Notes  
---|---|---  
${card.rows.map(r=>` ${r[0]} | ${r[1]} | ${r[2]}  
`).join('')} 

`; } return `

${card.title}

${card.sub}

▼

${card.code}

${card.output ? `

Expected output

${card.output}

` : ''} ${card.insight ? `

**Insight:** ${card.insight}

` : ''} 

`; } function setTab(t){ activeTab=t; renderNav(); renderContent(); } function tog(k){ openCards[k]=!openCards[k]; renderContent(); } document.getElementById('search').addEventListener('input', e => { searchTerm = e.target.value; renderContent(); }); renderNav(); renderContent();
